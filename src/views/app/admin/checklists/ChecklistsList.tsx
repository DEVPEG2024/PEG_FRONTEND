import { Container } from '@/components/shared';
import ChecklistsListContent from './ChecklistsListContent';

function ChecklistsList() {
  return (
    <Container className="h-full">
      <ChecklistsListContent />
    </Container>
  );
}

export default ChecklistsList;
